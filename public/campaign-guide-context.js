(()=>{
'use strict';
const params=new URLSearchParams(location.search),campaignId=params.get('id')||localStorage.getItem('oraclerpg.activeCampaignId')||'';if(!campaignId)return;
const en=localStorage.getItem('oraclerpg.locale')==='en';
const readSnapshot=()=>{try{return JSON.parse(localStorage.getItem(`oraclerpg.campaign.${campaignId}.v1`)||'null')}catch{return null}};
const clone=v=>{try{return typeof structuredClone==='function'?structuredClone(v):JSON.parse(JSON.stringify(v))}catch{return v}};
const directive=en?'Use the attached adventure PDF as the primary campaign guide from its opening scene. Follow its locations, encounters, NPCs, clues, pacing, and suggested rewards when they become relevant. Treat XP, currency, items, milestones, and other rewards as guidance and apply them through OracleCampaignState/OracleCampaignTimeline so every gain belongs to a rollback checkpoint. Adapt the guide when necessary to the actual character and campaign state; never grant a reward merely because it appears later in the PDF.':'Use o PDF de aventura anexado como guia principal da campanha desde a cena inicial. Siga seus locais, encontros, NPCs, pistas, ritmo e recompensas sugeridas quando elas se tornarem relevantes. Trate XP, dinheiro, itens, marcos e outras recompensas como orientação e aplique cada ganho por OracleCampaignState/OracleCampaignTimeline para que tudo pertença a um checkpoint reversível. Adapte o guia quando necessário ao personagem e ao estado real da campanha; nunca conceda uma recompensa apenas porque ela aparece mais adiante no PDF.';
async function guideFiles(){if(!window.OracleCampaignDocuments?.getGuideFiles)return[];return window.OracleCampaignDocuments.getGuideFiles()}
window.OracleCampaignGuide=Object.freeze({campaignId,isEnabled(){return Boolean(readSnapshot()?.draft?.adventureGuidePolicy?.enabled)},getMetadata(){return clone(readSnapshot()?.draft?.adventureGuide||null)},getPolicy(){return clone(readSnapshot()?.draft?.adventureGuidePolicy||null)},getMasterDirective(){return directive},async getFiles(){const meta=readSnapshot()?.draft?.adventureGuide;const files=await guideFiles();return meta?.id?files.filter(x=>x?.metadata?.id===meta.id):files},async getContext(){return{metadata:this.getMetadata(),policy:this.getPolicy(),directive,files:await this.getFiles()}}});
})();
