import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import {spawnSync} from 'node:child_process';import {fileURLToPath} from 'node:url';const here=path.dirname(fileURLToPath(import.meta.url)),content=path.resolve(here,'..'),repo=path.resolve(content,'../..'),script=path.join(content,'scripts/build-import-engine-browser.mjs'),out=path.join(repo,'public/oracle-import-engine');test('browser build publishes import engine modules used by homebrew creator',()=>{const r=spawnSync(process.execPath,[script],{encoding:'utf8'});assert.equal(r.status,0,r.stderr||r.stdout);for(const f of ['index.mjs','markdown-adapter.mjs','5etools-adapter.mjs','class-compiler.mjs','domain-compilers.mjs','ir.mjs'])assert.ok(fs.existsSync(path.join(out,f)),f);const bridge=fs.readFileSync(path.join(repo,'public/oracle-homebrew-import-bridge.js'),'utf8');assert.match(bridge,/oracle-import-engine\/index\.mjs/);assert.match(bridge,/saveHomebrew/)});
